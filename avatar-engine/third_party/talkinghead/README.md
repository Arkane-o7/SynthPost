# TalkingHead dependency

Avatar-Engine uses the published `@met4citizen/talkinghead` npm package rather
than copying the upstream application. The pinned dependency is declared in
`web_avatar_runtime/package.json`; attribution is in `third_party/NOTICE.md`.
